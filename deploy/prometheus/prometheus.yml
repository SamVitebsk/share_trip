global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "sharetrip"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:9090"]

  - job_name: "postgres"
    static_configs:
      - targets: ["postgres_exporter:9187"]
